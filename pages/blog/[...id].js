import { useRouter } from "next/router";

function BlogPosts() {
  const router = useRouter();

  return (
    <div>
      <h1>The Blog Post Catchall</h1>
    </div>
  );
}

export default BlogPosts;
